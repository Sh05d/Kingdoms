package com.kingdom;

import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.assertTrue;

/**
 * Placeholder test that establishes the JUnit 5 test source folder (src/test/java).
 * It does NOT load the Spring context, so it runs green without a database.
 * Replace / add real unit + slice tests here as the flows are built.
 */
class PlaceholderTest {

    @Test
    void sanityCheck() {
        assertTrue(true);
    }
}
