# Maven build output
target/

# IDE
.idea/
*.iml
*.iws
*.ipr
.vscode/
.settings/
.project
.classpath

# OS
.DS_Store
Thumbs.db

# Logs
*.log
src/main/resources/application.properties

# Local secrets (OpenAI key, Twilio token, Mailtrap token, etc.) — never commit
src/main/resources/application-local.properties

# Consolidated deployment secrets reference — LIVE KEYS, never commit
apis.md
apis

# IntelliJ HTTP Client — local secrets for manual API testing (Neotek creds)
http-client.private.env.json

# Throwaway test certificate PDFs (Volunteer kingdom manual testing)
certificate.pdf
volunteer-pdfs/
